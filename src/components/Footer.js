import React from "react";

export default function Footer() {
  var style = {
    backgroundColor: 'black',
    color: 'white',
    textAlign: 'center',
    padding: '10px',
    position: 'fixed',
    left: '0',
    bottom: '0',
    height: '50px',
    width: '100%',
  };

  return <div style={style}>FOOTER COMPONENT</div>;
}
